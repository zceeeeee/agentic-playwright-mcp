7.13 
## 问题：
程序前端：api配置和模型配置合到一起，api-key设置中间部分不可见（安全）
技能管理改成网站排序，技能多的排前面；技能管理页加一个搜索框；点击技能可把对应命令格式导入到聊天窗口
历史对话在删除后，会导致暂时无法输入；对历史对话编辑按键无反应，不能选择历史对话；历史对话名称要可编辑
浏览器设置加入：循环步数、clockbrowser启用
关于产品加入：github仓库地址，加一点描述

现在，对话中回答的输入框按enter键无法提交回答而是换行，改成”enter“提交回答，”ctrl+enter“换行；对话中点击输入框旁边的取消任务按键后该任务没有停止，需要自己点击停止任务，应该改成：非必填任务按下后选择默认值，必填任务按一次“必填“闪烁变红，按两次停止任务。
输入任务内容的输入框应该像命令行的那种，上下箭头可以切换上次或下一次的命令
登录逻辑有误，选择不使用登录信息还是会使用已有的登录账号
大窗口（桌面智能体控制台）和小球点开的窗口，所在的任务应该统一；切换任务默认停止上一个任务并关闭打开的浏览器。

聊天记录里，上一个任务的回答可能会跳到当前任务范围里

多任务设置修改：命令通过换行进行多个命令输入
## 目标：
初步了解新架构，使得新程序能跑起来。熟悉一下这个阶段的wps和微信操作

7.14
## 问题
关键词有误，
